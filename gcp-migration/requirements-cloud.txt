# Optimized dependencies for Cloud Run deployment 2025
# Minimal set for standalone MCP server without heavy RAG dependencies

# Core FastAPI stack
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Essential utilities
aiofiles>=23.0.0
httpx>=0.25.0
python-multipart>=0.0.6

# Google Cloud integration (lightweight)
google-cloud-logging>=3.8.0

# Performance monitoring
prometheus-client>=0.19.0

# Optional RAG dependencies (only loaded if available)
# These will be imported conditionally in the standalone server
chromadb>=0.4.0
sentence-transformers>=2.2.0
ollama>=0.1.0

# Document processing (lightweight)
markdown>=3.5.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0