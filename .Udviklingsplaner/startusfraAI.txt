Hej Trae,

Vi fortsætter med Action 3.3 fra `revisedplan.txt`. [cite: 40, 96] Vi har besluttet, at felterne `tags`, `categories`, `image`, og `banner` **skal** være en del af `EducationProgram`-modellen.

Udfør venligst følgende trin for at implementere denne ændring:

1.  **Opdatér Prisma Schema:**
    * Modificer `EducationProgram`-modellen i filen `apps/api/prisma/schema.prisma`.
    * Tilføj følgende felter:
        * `description String?` (hvis det ikke allerede eksisterer, overvej at tilføje det)
        * `tags String[] @default([])`
        * `categories String[] @default([])`
        * `image String?`
        * `banner String?`

2.  **Kør Database Migration:**
    * Udfør kommandoen: `npx prisma migrate dev --name add-fields-to-educationprogram`
    * Sørg for, at migrationen genereres korrekt.

3.  **Generer Prisma Client:**
    * Udfør kommandoen: `npx prisma generate`

4.  **Opdatér Service Lag (`EducationProgramService`):**
    * Åbn filen `apps/api/src/controllers/services/education-program.service.ts`.
    * I `createEducationProgram` metoden: Sørg for, at de nye felter (`tags`, `categories`, `image`, `banner`, og evt. `description`) korrekt overføres til `this.create()` kaldet.
    * I `updateEducationProgram` metoden: Fjern udkommenteringen for `tags`, `categories`, `image`, og `banner`, så disse felter kan opdateres. [cite: 78] Sørg for, at logikken korrekt håndterer opdatering af disse felter (både hvis en ny værdi angives, og hvis den ikke gør, så den eksisterende bevares).
    * Sikr dig, at den generiske `BaseService` (`apps/api/src/common/services/base.service.ts`), som `EducationProgramService` arver fra, kan håndtere disse felttyper korrekt ved oprettelse og opdatering (især array-typer som `tags` og `categories`).

5.  **Verificer DTO'er:**
    * Gennemgå `apps/api/src/controllers/dto/education-program/education-program.dto.ts`.
    * Sikr at `EducationProgramDto`, `CreateEducationProgramDto`, og `UpdateEducationProgramDto` er i overensstemmelse med de nye felter på modellen og i servicelaget. Felterne er allerede til stede i DTO'erne, så det handler primært om at bekræfte, at de stadig passer.

Lever venligst de nødvendige patches/diffs for alle ændrede filer. Efter implementeringen fortsætter vi med test og validering som beskrevet i Phase 3 af `revisedplan.txt`. [cite: 96]